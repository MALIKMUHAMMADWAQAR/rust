fn main() {
    //scalar data types are integer,float,char & boolean ---> lies under primitive data types
    //So we don't have to mention their types such as float64 or int 32
    
    let marks = 2;
    let grade = "A";
    let percentage = 86.5;
    let passed = true;
}
