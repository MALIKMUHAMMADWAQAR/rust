fn main() {
    //when we have negative values we have to declare that its a signed variables using formats i8,i16,i32,i64
    //same for unsigned u8,u16,u32,u64

    //as age cann't be negative so
    let age:u8 = 20;
    println!("{}",age);

    //temperature can be negative and positive so use signed variable

    let temperature:i16 = -4;
    println!("{}",temperature);
    


}
